<%_if(schema) {-%>
<%_switch(schema.sheetType){
case 1: { -%>
<%=`export enum ${schema.name}{` %>
<% schema.attrs.forEach((attr,index)=>{-%>
<%=include('ts_field',{type:schema.sheetType, attr, util})-%>
<%=`${index != schema.attrs.length - 1 ? '\n':''}`-%>
<%})-%>
<%='}'%>
<% break }-%> 
<% default: {-%>
<%=`export class ${schema.name} {` %>
<% schema.attrs.forEach((attr,index)=>{-%>
<%=include('ts_field',{type:schema.sheetType, attr, util})-%>
<%=`${index != schema.attrs.length - 1 ? '\n':''}`-%>
<% }) -%>
<%='}'%>
<% break }-%> 
<% } -%>
<% } -%>